<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= content_for?(:title) ? yield(:title) : "Columbine Therapy · Compassion. Curiosity. Change." %></title>
  <meta name="description" content="<%= content_for?(:meta_description) ? yield(:meta_description) : 'Therapy for individuals and couples in Colorado. Relational EMDR, psychodynamic, CBT/ACT, and mindfulness.' %>">

  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="theme-color" content="#6d9e8d">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="icon" type="image/png" href="/favicon.png">
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://api.fontshare.com/v2/css?f[]=ranade@300,400,500,600,700&display=swap" rel="stylesheet">

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", type: "module", "data-turbo-track": "reload" %>

  <%= yield :head %>
</head>

    <body class="min-h-dvh text-black antialiased" style="background-color: #FAFAFA;">
<header class="mx-auto max-w-5xl px-4 sm:px-6" role="banner">
  <div class="flex items-center justify-between py-4 sm:py-6">
    <!-- Logo on the left -->
    <div class="flex-shrink-0">
      <%= link_to root_path, class: "block" do %>
        <%= image_tag "SVG File-06.svg", alt: "Columbine Therapy", class: "h-20 sm:h-24 lg:h-28 w-auto" %>
      <% end %>
    </div>
    
    <!-- Navigation on the right -->
    <div class="flex-1">
      <%= render "shared/nav" %>
    </div>
  </div>
  <a href="#main" class="sr-only">Skip to content</a>
  <a id="main" class="sr-only"></a>
</header>

<main class="mx-auto max-w-5xl px-4 sm:px-6 py-8 sm:py-10" role="main">
  <div id="flash">
    <%= render "shared/flash_messages" %>
  </div>
  <%= yield %>
</main>

<footer class="mx-auto max-w-5xl px-4 sm:px-6" role="contentinfo">
  <%= render "shared/license_logos" %>
  <%= render "shared/footer" %>
  </footer>
</body>
</html>
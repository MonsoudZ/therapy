<!DOCTYPE html>
<html>
  <head>
    <title>Admin Panel | Clinical Psychologist</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              'sans': ['Inter', 'ui-sans-serif', 'system-ui', 'sans-serif'],
            }
          }
        }
      }
    </script>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body class="min-h-screen bg-gray-100">
    <div class="flex">
      <!-- Sidebar -->
      <div class="w-64 bg-gray-800 text-white min-h-screen">
        <div class="p-6">
          <h1 class="text-xl font-bold">Admin Panel</h1>
          <p class="text-gray-300 text-sm mt-1">Content Management</p>
        </div>
        
        <nav class="mt-6">
          <%= link_to "Dashboard", admin_root_path, 
              class: "block px-6 py-3 hover:bg-gray-700 #{'bg-gray-700' if current_page?(admin_root_path)}" %>
          <%= link_to "Site Content", admin_site_contents_path, 
              class: "block px-6 py-3 hover:bg-gray-700 #{'bg-gray-700' if current_page?(admin_site_contents_path)}" %>
          <%= link_to "Contact Requests", admin_contact_requests_path, 
              class: "block px-6 py-3 hover:bg-gray-700 #{'bg-gray-700' if current_page?(admin_contact_requests_path)}" %>
          <%= link_to "View Site", root_path, target: "_blank", 
              class: "block px-6 py-3 hover:bg-gray-700" %>
        </nav>
        
        <div class="absolute bottom-6 left-6 right-6">
          <%= link_to "Logout", destroy_admin_session_path, method: :delete, 
              class: "block w-full text-center bg-red-600 hover:bg-red-700 px-4 py-2 rounded" %>
        </div>
      </div>
      
      <!-- Main content -->
      <div class="flex-1">
        <div class="bg-white shadow-sm border-b">
          <div class="px-6 py-4">
            <h2 class="text-2xl font-semibold text-gray-800">
              <%= content_for(:page_title) || "Admin Dashboard" %>
            </h2>
          </div>
        </div>
        
        <main class="p-6">
          <% if notice.present? %>
            <div class="mb-6 rounded-lg border border-green-200 bg-green-50 px-4 py-3 text-green-800">
              <%= notice %>
            </div>
          <% end %>
          <% if alert.present? %>
            <div class="mb-6 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-red-800">
              <%= alert %>
            </div>
          <% end %>
          
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>
